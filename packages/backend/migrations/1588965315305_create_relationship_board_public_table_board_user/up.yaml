- args:
    name: board
    table:
      name: board_user
      schema: public
    using:
      manual_configuration:
        column_mapping:
          board_id: id
        remote_table:
          name: board
          schema: public
  type: create_object_relationship
