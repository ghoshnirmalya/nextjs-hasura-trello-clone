- args:
    name: labels
    table:
      name: card
      schema: public
    using:
      manual_configuration:
        column_mapping:
          id: card_id
        remote_table:
          name: label
          schema: public
  type: create_array_relationship
