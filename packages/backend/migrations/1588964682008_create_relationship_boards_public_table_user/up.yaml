- args:
    name: boards
    table:
      name: user
      schema: public
    using:
      manual_configuration:
        column_mapping:
          id: user_id
        remote_table:
          name: board_user
          schema: public
  type: create_array_relationship
