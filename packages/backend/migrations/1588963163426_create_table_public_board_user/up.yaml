- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."board_user"("board_id" uuid NOT NULL, "user_id" uuid
      NOT NULL, PRIMARY KEY ("board_id","user_id") );
  type: run_sql
- args:
    name: board_user
    schema: public
  type: add_existing_table_or_view
