- args:
    name: boards
    table:
      name: user
      schema: public
    using:
      manual_configuration:
        column_mapping:
          id: admin_id
        remote_table:
          name: board
          schema: public
  type: create_array_relationship
