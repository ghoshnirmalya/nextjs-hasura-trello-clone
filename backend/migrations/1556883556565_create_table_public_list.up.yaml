- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."list"("id" uuid NOT NULL UNIQUE DEFAULT gen_random_uuid(),
      "name" text NOT NULL, "position" integer NOT NULL, "board_id" uuid NOT NULL,
      PRIMARY KEY ("id") , FOREIGN KEY ("board_id") REFERENCES "public"."board"("id")
      ON UPDATE restrict ON DELETE restrict);
  type: run_sql
- args:
    name: list
    schema: public
  type: add_existing_table_or_view
