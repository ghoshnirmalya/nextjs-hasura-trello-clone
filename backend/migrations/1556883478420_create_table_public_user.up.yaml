- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."user"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "username" text NOT NULL, "email" text NOT NULL, "password" text NOT NULL, "role" text NOT NULL, "created_at"
      timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT
      now(), PRIMARY KEY ("id") , UNIQUE ("username"), UNIQUE ("id"), UNIQUE ("email"));
  type: run_sql
- args:
    name: user
    schema: public
  type: add_existing_table_or_view
