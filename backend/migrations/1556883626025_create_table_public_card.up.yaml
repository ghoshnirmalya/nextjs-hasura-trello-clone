- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."card"("id" uuid NOT NULL UNIQUE DEFAULT gen_random_uuid(),
      "description" text NOT NULL, "position" integer NOT NULL, "list_id" uuid NOT
      NULL, PRIMARY KEY ("id") , FOREIGN KEY ("list_id") REFERENCES "public"."list"("id")
      ON UPDATE restrict ON DELETE restrict);
  type: run_sql
- args:
    name: card
    schema: public
  type: add_existing_table_or_view
