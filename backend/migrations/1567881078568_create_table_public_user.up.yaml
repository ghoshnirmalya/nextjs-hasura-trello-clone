- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."user"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "email" bpchar NOT NULL, "password" bpchar NOT NULL, "created_at" timestamptz,
      "updated_at" timestamptz NOT NULL DEFAULT now(), "active" boolean NOT NULL DEFAULT
      true, PRIMARY KEY ("id") , UNIQUE ("id"), UNIQUE ("email"));
  type: run_sql
- args:
    name: user
    schema: public
  type: add_existing_table_or_view
